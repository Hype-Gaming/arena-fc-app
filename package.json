{
  "name": "tips-app",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["api", "web"],
  "scripts": {
    "up": "docker compose up -d --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "test:infra": "node test/workspace.test.js && node test/infra.test.js && node test/compose.test.js"
  }
}
